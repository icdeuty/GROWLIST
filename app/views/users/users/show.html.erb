マイページ

<%= link_to 'プロフィールを編集する', edit_users_user_path(current_user), class: 'btn btn-primary'%>
<%= link_to 'フォロー/フォロワーを見る', users_relationships_path(current_user), class: 'btn btn-primary'%>


<h1>About me</h1>

<h3>ユーザー名</h3>
<%= @user.name %>
<h3>プロフィール</h3>
<%= @user.profile %>
<h3>プロフィール画像</h3>
<% if @user.profile_img.blank? %>
<%= attachment_image_tag @user, :profile_img_id, :fill, 60, 60, class: "img-circle pull-left profile-thumb", fallback: "no_image.jpg" %>
<% else %>
<%= image_tag attachment_url(@user, :profile_img, :fill, 300, 300, format: "jpg"),class: "img-circle pull-left profile-thumb" %>
<% end %>

<%= render partial: 'users/relationships/follow_button',locals: { user: @user } %>